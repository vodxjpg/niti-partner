# Niftipay Partners Docs

Static docs site for the Niftipay partner API. No framework — Markdown compiled to HTML.

## Develop
npm install
npm run build      # compiles content/ -> dist/
npm run dev        # serves dist/ on http://localhost:4173

## Deploy
Ship the `dist/` folder to partners.niftipay.com (GitHub Pages or any static host).
